<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Overtime User Group -->
        <record id="group_hr_overtime_user" model="res.groups">
            <field name="name">Overtime User</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
            <field name="implied_ids" eval="[(4, ref('hr.group_hr_user'))]"/>
        </record>

        <!-- Overtime Manager Group -->
        <record id="group_hr_overtime_manager" model="res.groups">
            <field name="name">Overtime Manager</field>
            <field name="category_id" ref="base.module_category_human_resources"/>
            <field name="implied_ids" eval="[(4, ref('group_hr_overtime_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        <!-- Record Rules -->
        <!-- Employees can see their own overtime records -->
        <record id="hr_overtime_rule_employee" model="ir.rule">
            <field name="name">Employee: see own overtime</field>
            <field name="model_id" ref="model_hr_overtime"/>
            <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Managers can see their team's overtime records -->
        <record id="hr_overtime_rule_manager" model="ir.rule">
            <field name="name">Manager: see team overtime</field>
            <field name="model_id" ref="model_hr_overtime"/>
            <field name="domain_force">['|', ('employee_id.user_id', '=', user.id), ('employee_id.parent_id.user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_hr_overtime_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- HR Managers can see all overtime records -->
        <record id="hr_overtime_rule_hr_manager" model="ir.rule">
            <field name="name">HR Manager: see all overtime</field>
            <field name="model_id" ref="model_hr_overtime"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_hr_overtime_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Multi-company rule -->
        <record id="hr_overtime_rule_multi_company" model="ir.rule">
            <field name="name">Overtime: multi-company</field>
            <field name="model_id" ref="model_hr_overtime"/>
            <field name="domain_force">[('company_id', 'in', company_ids)]</field>
            <field name="global" eval="True"/>
        </record>

    </data>

    <!-- Sequence for Overtime Reference -->
    <data noupdate="1">
        <record id="sequence_hr_overtime" model="ir.sequence">
            <field name="name">Overtime Sequence</field>
            <field name="code">hr.overtime</field>
            <field name="prefix">OT/%(year)s/</field>
            <field name="padding">5</field>
            <field name="company_id" eval="False"/>
        </record>
    </data>
</odoo>
